import { BaseContainer } from '../../components/base/base-container'
import { Header } from '../../components/Header'
import { PassedQuizzesBody } from '../../components/PassedQuizzesBody/'

const PassedQuizzes = () => {
  return (
    <BaseContainer>
      <Header />
      <PassedQuizzesBody />
    </BaseContainer>
  )
}

export default PassedQuizzes
